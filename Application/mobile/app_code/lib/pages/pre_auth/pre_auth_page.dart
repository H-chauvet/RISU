import 'package:flutter/material.dart';

import 'pre_auth_state.dart';

class PreAuthPage extends StatefulWidget {
  const PreAuthPage({Key? key}) : super(key: key);

  @override
  State<PreAuthPage> createState() => PreAuthState();
}
