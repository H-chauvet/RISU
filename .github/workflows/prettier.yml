name: Code quality check

on: push

jobs:
  Prettier-WEB:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Download dependencies
        run: |
          npm install prettier-plugin-dart prettier
      - name: run prettier
        working-directory: ./Configurateur/front/lib
        run: |
          dart format './**/*'
